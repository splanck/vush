#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "alias hi='echo hi'\r"
expect "vush> "
send "unalias -a\r"
expect "vush> "
send "alias\r"
expect {
    -re "\[\r\n\]+vush> " {}
    timeout { send_user "unalias -a failed\n"; exit 1 }
}
send "exit\r"
expect eof
