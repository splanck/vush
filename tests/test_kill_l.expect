#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "kill -l\r"
expect {
    -re "TERM" {}
    timeout { send_user "kill -l output mismatch\n"; exit 1 }
}
expect "vush> "
send "exit\r"
expect eof
