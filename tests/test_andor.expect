#!/usr/bin/expect -f
set timeout 5
spawn ../vush
expect "vush> "
send "false && echo nope\r"
expect {
    -re "nope" { send_user "AND execution failed\n"; exit 1 }
    -re "vush> " {}
    timeout { send_user "command timeout\n"; exit 1 }
}
send "exit\r"
expect eof
