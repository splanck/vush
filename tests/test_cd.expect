#!/usr/bin/expect -f
set timeout 5
spawn ../vush
expect "vush> "
send "cd /\r"
expect "vush> "
send "pwd\r"
expect {
    -re "[\r\n]+/[\r\n]+vush> " {}
    timeout { send_user "cd output mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
