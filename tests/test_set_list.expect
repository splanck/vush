#!/usr/bin/expect -f
set timeout 5
spawn ../vush
expect "vush> "
send "FOO=bar\r"
expect "vush> "
send "set\r"
expect {
    -re "FOO=bar" {}
    timeout { send_user "set did not list variable\n"; exit 1 }
}
send "exit\r"
expect eof
