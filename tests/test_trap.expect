#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "trap 'echo trapped' INT\r"
expect "vush> "
set pid [exp_pid]
exec kill -INT $pid
expect {
    -re "[\r\n]+trapped[\r\n]+vush> " {}
    timeout { send_user "trap output mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
