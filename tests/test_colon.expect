#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send ":\r"
expect "vush> "
send "echo $?\r"
expect {
    -re "[\r\n]+0[\r\n]+vush> " {}
    timeout { send_user ": status mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
