#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "pwd\r"
expect {
    -re "[\r\n]+$env(PWD)[\r\n]+vush> " {}
    timeout { send_user "pwd output mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
