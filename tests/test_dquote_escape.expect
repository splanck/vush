#!/usr/bin/env expect
set timeout 5
spawn [file dirname [info script]]/../vush -c {echo \"${HOME}\"}
expect {
    -re "\"$env(HOME)\"\r?\n" {}
    eof {}
    timeout { send_user "escaped quote expansion failed\n"; exit 1 }
}
