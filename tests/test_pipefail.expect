#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "set -o pipefail\r"
expect "vush> "
send "false | true\r"
expect "vush> "
send "echo $?\r"
expect {
    -re "[\r\n]+1[\r\n]+vush> " {}
    timeout { send_user "pipefail status mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
