#!/usr/bin/expect -f
set timeout 5
spawn ../vush
expect "vush> "
send "read foo\r"
send \004
expect "vush> "
send "echo $?\r"
expect {
    -re "[\r\n]+1[\r\n]+vush> " {}
    timeout { send_user "read EOF status failed\n"; exit 1 }
}
send "exit\r"
expect eof
