#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "cat <<-EOF\r"
send "\tfoo\r"
send "EOF\r"
expect {
    -re "\[\r\n\]+foo\[\r\n\]+vush> " {}
    timeout { send_user "heredoc <<- failed\n"; exit 1 }
}
send "exit\r"
expect eof
