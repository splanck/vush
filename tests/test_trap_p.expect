#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "trap 'echo hi' INT\r"
expect "vush> "
send "trap -p\r"
expect {
    -re "\[\r\n\]+trap 'echo hi' INT\[\r\n\]+vush> " {}
    timeout { send_user "trap -p output mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
