#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "false\r"
expect "vush> "
send "echo \$?\r"
expect {
    -re "[\r\n]+1[\r\n]+vush> " {}
    timeout { send_user "false builtin status mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
