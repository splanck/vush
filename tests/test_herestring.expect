#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "cat <<<\"hello\"\r"
expect {
    -re "\[\r\n\]+hello\[\r\n\]+vush> " {}
    timeout { send_user "here-string failed\n"; exit 1 }
}
send "exit\r"
expect eof
