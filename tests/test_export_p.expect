#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "export FOO=bar\r"
expect "vush> "
send "export -p\r"
expect {
    -re "export FOO='bar'" {}
    timeout { send_user "export -p output mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
