#!/usr/bin/expect -f
set timeout 5
spawn ../vush
expect "vush> "
send "readonly FOO=bar\r"
expect "vush> "
send "readonly -p\r"
expect {
    -re "readonly FOO=bar" {}
    timeout { send_user "readonly -p output mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
