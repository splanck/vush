#!/usr/bin/expect -f
set timeout 5
spawn ../vush
expect "vush> "
send "cat <<EOF\r"
send "hello\r"
send "world\r"
send "EOF\r"
expect {
    -re "[\r\n]+hello[\r\n]+world[\r\n]+vush> " {}
    timeout { send_user "heredoc output mismatch\n"; exit 1 }
}
send "exit\r"
expect eof

