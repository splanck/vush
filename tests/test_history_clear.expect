#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
send "history -c\r"
expect "vush> "
send "history\r"
expect {
    -re "\[\r\n\]+vush> " {}
    timeout { send_user "history not cleared\n"; exit 1 }
}
send "exit\r"
expect eof
