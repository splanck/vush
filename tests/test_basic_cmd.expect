#!/usr/bin/env expect
set timeout 5
spawn ../vush
expect "vush> "
# run a basic command and ensure output is printed
send "echo hi\r"
expect {
    -re "\[\r\n\]+hi\[\r\n\]+vush> " {}
    timeout { send_user "basic command output mismatch\n"; exit 1 }
}
send "exit\r"
expect eof
